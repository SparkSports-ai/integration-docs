---
title: Transaction lifecycle
description: How bets, wins, and rollbacks flow through a Spark game round.
---

Each round gets a `roundId`. Most rounds have one `BET` and one `WIN` or `ROLLBACK`. A losing round still ends with a `WIN` of amount `0`, flagged so it reads as a close, not a payout.

Amounts are decimal strings and every wallet transaction carries `currency`. The transaction currency must match the player session currency and the original `BET`.

{/* v1: added amount/currency invariant to lifecycle. WHY: wallet-api had the rule, but lifecycle is where reviewers reason about round state; currency mismatch is a settlement bug, not just a field-validation bug. */}

## Flow

{/* v1: was an ASCII tree. WHY: replaced with a Mermaid flowchart (renders to SVG at build time, see how-it-works) so every flow on the site is a real diagram. Types are words; the loss path now shows reason=loss + amount 0 instead of a bare "Win, amount=0". */}

```mermaid
flowchart TD
    A[Player starts streak] --> B[BET: debit stake]
    B --> C[Player picks live markets]
    C -->|Loss| D["WIN amount 0, reason=loss, roundFinished=true"]
    C -->|Cashout| E["WIN payout, reason=cashout"]
    C -->|Cap reached| F["WIN capped payout, reason=cap"]
    C -->|Error or void| G["ROLLBACK: refund the BET"]
    D --> H[Round done]
    E --> H
    F --> H
    G --> H
```

## Transaction types

{/* v1: numeric Type column dropped; words only. */}

| Type | Action | When |
| --- | --- | --- |
| `BET` | Debit | New streak starts |
| `WIN` | Credit | Cashout, cap, or losing close (amount `0`) |
| `ROLLBACK` | Refund | A `BET` must be reversed |

## Linking settlements to the bet

{/* v1: NEW section. WHY: previously nothing tied a WIN/ROLLBACK back to its BET, so reconciliation relied on roundId alone. */}

A `WIN` or `ROLLBACK` carries `betTransactionId`, the `transactionId` of the `BET` it settles or reverses. Use it to reconcile, and to make sure a `ROLLBACK` reverses exactly the right debit.

After a round has a closing `WIN` or `ROLLBACK`, another settlement for the same `BET` must not move money. Return `ROUND_ALREADY_SETTLED`.

If the transaction currency does not match the session or original `BET`, return `CURRENCY_MISMATCH`.

See the full [Wallet API settlement rules](/docs/direct-integration/wallet-api#settlement-rules).

{/* v1: synced lifecycle with wallet-api settlement errors and linked to the canonical rules block. WHY: the lifecycle page should state the same terminal-state and currency rules as the wallet error table, otherwise partners can implement the flow without noticing the rejection cases. */}

## Typical sequence

1. Player opens Spark. Your backend calls [Launch](/docs/direct-integration/launch).
2. We call your [Session validation](/docs/direct-integration/session-validation).
3. Player starts a streak. `BET` debits the stake.
4. Player picks through live steps.
5. Cashout, cap, or loss: `WIN` credits the payout (`"0"` on loss), with `reason` and `roundFinished`.
6. Void or error: `ROLLBACK` refunds the original `BET`, referenced by `betTransactionId`.

## Retries

We retry wallet calls when the failure looks transient (`5xx` or timeout). Your handler must be idempotent on `transactionId` so a retry never double-charges or double-credits. If a timed-out call later applies, the retry must return the applied response rather than an unknown result. See [Wallet API retries](/docs/direct-integration/wallet-api#retries).

{/* v1: added timeout-after-apply rule to lifecycle. WHY: this is the money-path retry invariant reviewers need to see even before reading the detailed wallet page. */}
